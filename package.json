{
  "name": "my-portfolio",
  "private": true,
  "version": "1.0.0",
  "description": "Modern developer portfolio with a protected admin portal, Express/Firebase backend and Vite/React frontend.",
  "workspaces": [
    "server",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install && npm install --workspace server && npm install --workspace frontend",
    "dev": "concurrently -n \"SERVER,FRONTEND\" -c \"magenta,cyan\" \"npm:dev:server\" \"npm:dev:frontend\"",
    "dev:server": "npm run dev --workspace server",
    "dev:frontend": "npm run dev --workspace frontend",
    "build": "npm run build --workspace server && npm run build --workspace frontend",
    "build:server": "npm run build --workspace server",
    "build:frontend": "npm run build --workspace frontend",
    "start": "npm run start --workspace server",
    "lint": "npm run lint --workspace frontend"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}